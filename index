<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Killian's Super Awesome Page</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg1: #ff9a9e;
      --bg2: #fad0c4;
      --bg3: #fbc2eb;
      --bg4: #a18cd1;
      --accent1: #ff6b6b;
      --accent2: #ffd93d;
      --accent3: #4ecdc4;
      --accent4: #1a8cff;
      --text-dark: #1f2335;
      --white: #ffffff;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      min-height: 100vh;
      background: radial-gradient(circle at top left, var(--bg1), var(--bg3)),
                  radial-gradient(circle at bottom right, var(--bg2), var(--bg4));
      background-blend-mode: screen;
      color: var(--text-dark);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 16px;
      overflow-x: hidden;
    }

    .bubbles {
      position: fixed;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
      z-index: 0;
    }

    .bubble {
      position: absolute;
      width: 80px;
      height: 80px;
      border-radius: 50%;
      opacity: 0.18;
      animation: float 18s linear infinite;
      background: radial-gradient(circle at 30% 30%, #ffffff, transparent);
    }

    .bubble.b1 { background-color: #ff6b6b; top: 80%; left: 5%; animation-duration: 20s; }
    .bubble.b2 { background-color: #ffd93d; top: 90%; left: 40%; animation-duration: 24s; }
    .bubble.b3 { background-color: #4ecdc4; top: 85%; left: 75%; animation-duration: 22s; }
    .bubble.b4 { background-color: #1a8cff; top: 95%; left: 20%; animation-duration: 26s; }

    @keyframes float {
      0% { transform: translateY(0) translateX(0) scale(1); }
      50% { transform: translateY(-40vh) translateX(15px) scale(1.2); }
      100% { transform: translateY(-80vh) translateX(-10px) scale(1); }
    }

    .card {
      position: relative;
      z-index: 1;
      background: rgba(255, 255, 255, 0.85);
      border-radius: 32px;
      padding: 24px 20px 28px;
      max-width: 900px;
      width: 100%;
      box-shadow:
        0 16px 40px rgba(0, 0, 0, 0.18),
        0 0 0 2px rgba(255, 255, 255, 0.4);
      backdrop-filter: blur(16px);
    }

    @media (min-width: 768px) {
      .card {
        padding: 32px 32px 36px;
      }
    }

    .title-area {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 8px;
      text-align: center;
      margin-bottom: 24px;
    }

    .eyebrow {
      font-size: 0.9rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      font-weight: 700;
      color: rgba(31, 35, 53, 0.7);
    }

    .title {
      font-size: clamp(2.4rem, 6vw, 3.6rem);
      font-weight: 900;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: var(--text-dark);
      text-shadow:
        0 4px 0 #ffd93d,
        0 8px 0 #ff6b6b,
        0 12px 18px rgba(0, 0, 0, 0.4);
    }

    .title span {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 999px;
      background: linear-gradient(135deg, #ff6b6b, #ffd93d, #1a8cff);
      -webkit-background-clip: text;
      color: transparent;
    }

    .subtitle {
      font-size: 1rem;
      max-width: 460px;
      color: rgba(31, 35, 53, 0.85);
    }

    .fun-row {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      justify-content: center;
      margin-bottom: 24px;
    }

    .chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      border-radius: 999px;
      font-size: 0.9rem;
      background: rgba(255, 255, 255, 0.9);
      box-shadow: 0 4px 10px rgba(0,0,0,0.12);
    }

    .chip-emoji {
      font-size: 1.1rem;
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 16px;
    }

    @media (min-width: 640px) {
      .grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (min-width: 960px) {
      .grid {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
    }

    .tile {
      position: relative;
      padding: 16px 14px 18px;
      border-radius: 24px;
      background: linear-gradient(135deg, rgba(255,255,255,0.9), rgba(255,255,255,0.6));
      cursor: pointer;
      overflow: hidden;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.18);
      transform: translateY(0);
      transition:
        transform 0.18s ease-out,
        box-shadow 0.18s ease-out,
        background 0.18s ease-out;
    }

    .tile:hover {
      transform: translateY(-4px) rotate(-0.5deg);
      box-shadow: 0 12px 22px rgba(0, 0, 0, 0.28);
      background: linear-gradient(135deg, rgba(255,255,255,1), rgba(255,255,255,0.9));
    }

    .tile:active {
      transform: translateY(1px) scale(0.98);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    .tile-label {
      font-weight: 800;
      font-size: 1.2rem;
      margin-bottom: 4px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .tile-emoji {
      font-size: 1.6rem;
    }

    .tile-description {
      font-size: 0.9rem;
      opacity: 0.85;
      margin-bottom: 10px;
    }

    .tag {
      display: inline-flex;
      align-items: center;
      font-size: 0.75rem;
      padding: 4px 8px;
      border-radius: 999px;
      background: rgba(26, 140, 255, 0.08);
      border: 1px dashed rgba(26, 140, 255, 0.5);
      color: #0b4f91;
    }

    .tag span {
      margin-left: 4px;
    }

    .pill-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 20px;
      gap: 12px;
      flex-wrap: wrap;
    }

    .pill {
      font-size: 0.85rem;
      background: rgba(255,255,255,0.9);
      padding: 6px 12px;
      border-radius: 999px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.12);
    }

    .surprise-button {
      border: none;
      outline: none;
      border-radius: 999px;
      padding: 8px 16px;
      font-size: 0.9rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      cursor: pointer;
      background: linear-gradient(135deg, #ff6b6b, #ffd93d, #4ecdc4);
      color: #1f2335;
      box-shadow: 0 6px 14px rgba(0,0,0,0.25);
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transform: translateY(0);
      transition:
        transform 0.15s ease-out,
        box-shadow 0.15s ease-out,
        filter 0.15s ease-out;
    }

    .surprise-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.3);
      filter: brightness(1.05);
    }

    .surprise-button:active {
      transform: translateY(1px) scale(0.97);
      box-shadow: 0 4px 10px rgba(0,0,0,0.25);
    }

    .confetti-piece {
      position: fixed;
      width: 10px;
      height: 16px;
      border-radius: 4px;
      pointer-events: none;
      z-index: 999;
      opacity: 0.9;
      animation: confetti-fall 1200ms linear forwards;
    }

    @keyframes confetti-fall {
      0% {
        transform: translate3d(0, -100vh, 0) rotateZ(0deg);
      }
      100% {
        transform: translate3d(0, 100vh, 0) rotateZ(360deg);
      }
    }

    .footer {
      margin-top: 16px;
      font-size: 0.8rem;
      text-align: center;
      opacity: 0.7;
    }

    .goat {
      display: inline-block;
      transition: transform 0.18s ease-out;
      cursor: pointer;
    }

    .goat.bounce {
      animation: goat-bounce 600ms ease-out;
    }

    @keyframes goat-bounce {
      0% { transform: translateY(0) rotate(0deg); }
      30% { transform: translateY(-10px) rotate(-8deg); }
      60% { transform: translateY(0) rotate(6deg); }
      100% { transform: translateY(0) rotate(0deg); }
    }

    a.tile-link {
      color: inherit;
      text-decoration: none;
      display: block;
    }
  </style>
</head>
<body>
  <!-- Soft background bubbles -->
  <div class="bubbles" aria-hidden="true">
    <div class="bubble b1"></div>
    <div class="bubble b2"></div>
    <div class="bubble b3"></div>
    <div class="bubble b4"></div>
  </div>

  <!-- Main card -->
  <main class="card" role="main">
    <section class="title-area" aria-label="Welcome section">
      <div class="eyebrow">This laptop belongs to</div>
      <h1 class="title">
        <span>Killian</span>'s Super Awesome Page
      </h1>
      <p class="subtitle">
        Tap a big button to go straight to your cars, goats, or games. Have fun, buddy! 💖
      </p>
    </section>

    <div class="fun-row" aria-hidden="true">
      <div class="chip">
        <span class="chip-emoji">🚗</span> Hot Wheels Hero
      </div>
      <div class="chip">
        <span class="chip-emoji goat" id="goatEmoji">🐐</span> Goat Simulator Pro
      </div>
      <div class="chip">
        <span class="chip-emoji">🎮</span> Xbox Champion
      </div>
    </div>

    <!-- Big tiles -->
    <section class="grid" aria-label="Killian's shortcuts">
      <!-- Hot Wheels -->
      <article class="tile">
        <a class="tile-link" href="https://www.youtube.com/results?search_query=hot+wheels+kids" target="_blank" rel="noopener noreferrer">
          <div class="tile-label">
            <span>Hot Wheels Zone</span>
            <span class="tile-emoji">🚗</span>
          </div>
          <p class="tile-description">
            Watch crazy tracks, big jumps, and super-fast cars!
          </p>
          <div class="tag">
            ⭐ <span>Change this link to your playlist</span>
          </div>
        </a>
      </article>

      <!-- Goat Simulator / silly goat stuff -->
      <article class="tile" id="goatTile">
        <!-- Put your own Goat Simulator link here -->
        <a class="tile-link" href="https://store.steampowered.com/app/265930/Goat_Simulator/" target="_blank" rel="noopener noreferrer">
          <div class="tile-label">
            <span>Goat Time</span>
            <span class="tile-emoji">🐐</span>
          </div>
          <p class="tile-description">
            Bonk things, headbutt stuff, and be the silliest goat ever.
          </p>
          <div class="tag">
            🤪 <span>Mega silly mode</span>
          </div>
        </a>
      </article>

      <!-- Online little games -->
      <article class="tile">
        <!-- Swap this for your favorite kid-safe games hub -->
        <a class="tile-link" href="https://www.poki.com" target="_blank" rel="noopener noreferrer">
          <div class="tile-label">
            <span>Little Online Games</span>
            <span class="tile-emoji">🕹️</span>
          </div>
          <p class="tile-description">
            Fun tiny games you can play right in the browser.
          </p>
          <div class="tag">
            🧸 <span>Pick safe favorites here</span>
          </div>
        </a>
      </article>

      <!-- Xbox / “Game Time” area -->
      <article class="tile" id="xboxTile">
        <div class="tile-label">
          <span>Xbox Game Time</span>
          <span class="tile-emoji">🎮</span>
        </div>
        <p class="tile-description" id="xboxMessage">
          Tap this when you start Xbox — it will cheer you on!
        </p>
        <div class="tag">
          📣 <span>Go Killian!</span>
        </div>
      </article>

      <!-- Favorites / About Killian -->
      <article class="tile">
        <div class="tile-label">
          <span>About Killian</span>
          <span class="tile-emoji">⭐</span>
        </div>
        <p class="tile-description">
          Age: 6 · Loves: Hot Wheels, Goat Simulator, Xbox, and being awesome.
        </p>
        <div class="tag">
          💬 <span>Custom facts: edit in code</span>
        </div>
      </article>

      <!-- Surprise button -->
      <article class="tile">
        <div class="tile-label">
          <span>Magic Confetti</span>
          <span class="tile-emoji">🎉</span>
        </div>
        <p class="tile-description">
          Press the button for a quick happy confetti explosion!
        </p>
        <button class="surprise-button" id="surpriseButton">
          🎉 Surprise me!
        </button>
      </article>
    </section>

    <div class="pill-row">
      <div class="pill">
        Tip for grown-up: right-click → “Bookmark this page” so Killian can find it easily.
      </div>
      <button class="surprise-button" id="themeButton" type="button">
        🌈 Color Shuffle
      </button>
    </div>

    <p class="footer">
      Made with ❤️ just for Killian.
    </p>
  </main>

  <script>
    // Simple confetti
    const colors = ["#ff6b6b", "#ffd93d", "#4ecdc4", "#1a8cff", "#ff9ff3", "#feca57"];

    function launchConfetti() {
      const count = 80;
      const { innerWidth: width } = window;

      for (let i = 0; i < count; i++) {
        const confetti = document.createElement("div");
        confetti.className = "confetti-piece";
        const color = colors[Math.floor(Math.random() * colors.length)];
        confetti.style.backgroundColor = color;
        const left = Math.random() * width;
        confetti.style.left = left + "px";
        document.body.appendChild(confetti);

        confetti.addEventListener("animationend", () => {
          confetti.remove();
        });
      }
    }

    const surpriseButton = document.getElementById("surpriseButton");
    if (surpriseButton) {
      surpriseButton.addEventListener("click", launchConfetti);
    }

    // Goat bounce
    const goatEmoji = document.getElementById("goatEmoji");
    if (goatEmoji) {
      goatEmoji.addEventListener("click", () => {
        goatEmoji.classList.remove("bounce");
        void goatEmoji.offsetWidth; // restart animation
        goatEmoji.classList.add("bounce");
      });
    }

    // Xbox cheer messages
    const xboxTile = document.getElementById("xboxTile");
    const xboxMessage = document.getElementById("xboxMessage");
    const cheers = [
      "GAME TIME! You’ve got this, Killian! 💪",
      "New high score incoming… I can feel it. ⭐",
      "Controller ready. Brain ready. GO! 🎮",
      "You’re the Xbox boss, buddy! 👑",
      "No one can stop Killian the Great! 🚀"
    ];

    if (xboxTile && xboxMessage) {
      xboxTile.addEventListener("click", () => {
        const randomText = cheers[Math.floor(Math.random() * cheers.length)];
        xboxMessage.textContent = randomText;
        launchConfetti();
      });
    }

    // Tiny color shuffle for background
    const themeButton = document.getElementById("themeButton");
    if (themeButton) {
      themeButton.addEventListener("click", () => {
        const root = document.documentElement;
        const randomOffset = Math.floor(Math.random() * 360);
        root.style.setProperty(
          "--bg1",
          `hsl(${(10 + randomOffset) % 360}, 90%, 75%)`
        );
        root.style.setProperty(
          "--bg2",
          `hsl(${(40 + randomOffset) % 360}, 90%, 80%)`
        );
        root.style.setProperty(
          "--bg3",
          `hsl(${(300 + randomOffset) % 360}, 85%, 78%)`
        );
        root.style.setProperty(
          "--bg4",
          `hsl(${(220 + randomOffset) % 360}, 70%, 70%)`
        );
        launchConfetti();
      });
    }
  </script>
</body>
</html>
